import { defineConfig } from 'windicss/helpers'

export default defineConfig({
  theme: {
    extend: {
      colors: {
        primary: {
          extralight: '#ffffff',
          light: '#c4c4c4',
          DEFAULT: '#333333',
        },
      },
    }
  }
})